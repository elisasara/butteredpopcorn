import React from "react";

const Loading = () => (
    <div className="text-center" id="loadingSpinner">
        <h1>
        <i className="fa fa-spinner fa-spin"></i>
        </h1>
    </div>
);

export default Loading;